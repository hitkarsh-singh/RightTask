# Application
PORT=3000

# JWT
JWT_SECRET=your-secret-key-change-in-production

# Database (SQLite - no config needed, file-based)
# Database file will be created automatically as 'symbiotic-tasks.db'
